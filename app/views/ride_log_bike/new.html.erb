<%= form_for :ride_log_bike, :url => {:controller => "ride_log_bike", :action => "create"} do |f| %>

  Name: <%= f.text_field :name %>
  Details: <%= f.text_field :details %>

  <%= f.submit %>
<% end %>
