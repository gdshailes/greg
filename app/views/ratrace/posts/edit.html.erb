<div class='panel header'>
  <span>Hello, <%= current_user.display_name %>!</span>
  <ul>
    <li>
      <%= link_to 'Go Back', ratrace_posts_url %>
    </li>
  </ul>
</div>

<h1 class='panel-header'>Edit Post</h1>
<div class='panel-body edit-post'>
  <%= form_for @post do |f| %>
    <%= f.text_field :title, class: 'input' %>
    <%= f.text_area :body, class: 'input' %>
    <div class='actions'>
      <%= link_to 'Delete Post', @post, method: :delete, data: { confirm: 'Are you sure you want to delete this post?'} , class: 'lefty' %>
      <%= link_to 'Cancel', ratrace_posts_url %>
      <%= f.submit "Save" %>
    </div>
  <% end %>
</div>
